name: Download bestgirlsexy Albums

on:
  schedule:
    - cron: "10 * * * *"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag slug (from URL: bestgirlsexy.com/tag/XXX/)"
        required: true
        default: "shirato-hana"
      tag_id:
        description: "WordPress tag ID (leave 0 to auto-resolve)"
        required: false
        default: "0"
      list_only:
        description: "Only list albums, don't download"
        required: false
        type: boolean
        default: false

env:
  PYTHONIOENCODING: utf-8

jobs:
  download:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests beautifulsoup4

      - name: Run scraper
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
        run: |
          TAG="${{ inputs.tag || 'shirato-hana' }}"
          ARGS="--tag $TAG --output ./images/bestgirlsexy --incremental"

          if [ "${{ inputs.tag_id || '0' }}" != "0" ]; then
            ARGS="$ARGS --tag-id ${{ inputs.tag_id || '0' }}"
          fi

          if [ "${{ inputs.list_only }}" = "true" ]; then
            ARGS="$ARGS --list-only"
          fi

          python scraper_bestgirlsexy.py $ARGS

      - name: Commit images and manifest
        if: inputs.list_only == false
        continue-on-error: true
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add images/bestgirlsexy/ manifests/ || true
          if git diff --cached --quiet; then
            echo "No new images to commit"
          else
            new_files=$(git diff --cached --name-only --diff-filter=A | wc -l)
            git commit -m "bestgirlsexy: +${new_files} images [skip ci]"
            git push
          fi
